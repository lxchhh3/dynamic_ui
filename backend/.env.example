DATABASE_URL=postgresql+asyncpg://custom_ui:custom_ui_dev@127.0.0.1:5433/custom_ui
JWT_SECRET=change-me-in-prod-please
JWT_ALGORITHM=HS256
JWT_EXPIRE_DAYS=7
CORS_ORIGINS=http://localhost:5173
LOG_LEVEL=INFO
# Hybrid LLM (Phase 6). Unset = feature off (deterministic text).
# On the VPS with the 5070Ti reverse tunnel: http://127.0.0.1:8001/render
# LLM_RENDER_URL=
